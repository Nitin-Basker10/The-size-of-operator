#include <stdio.h>

int main() 
{
    int a;
    scanf("%d", &a);
    float b;
    scanf("%f", &b);
    double c;
    scanf("%lf", &c);
    long d;
    scanf("%ld", &d);
    printf("%zu %zu %zu %zu",sizeof(a),sizeof(b),sizeof(c),sizeof(d));
    
    
    return 0;
  
}
